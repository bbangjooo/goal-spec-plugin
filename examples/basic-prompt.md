# Basic Prompt

```text
Use $goal-spec to turn this objective into a goal-executable spec:

I want to build a reusable evaluation loop for improving our customer-support answer quality. The loop should take real support tickets, propose improvements, test them against a rubric, record failures, and keep improving the rubric without letting the model mark its own work as complete.
```

